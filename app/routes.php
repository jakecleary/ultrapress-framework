<?php

use Allsop\Route.php

/**
 * Define all your app routes in this file. Order them by specificity (high to
 * low) so that requests wont be captured by generic routes when they should
 * go to something more specific (e.g '/contact' should be picked up by the
 * 'getContact' PageController method, not the 'show' method.
 */
